#!/bin/bash

DOCK=$(xrandr | grep "DP-0 connected")

if [ -z "$DOCK" ]
then
	if [ -z $(xrandr | grep "HDMI-0 connected") ]
	then
		# nothing seems to be connect, so use just normal display setup
      		xrandrSetup
	else
		xrandrSetup --hdmi
	fi
else
      xrandrSetup --dock
fi

