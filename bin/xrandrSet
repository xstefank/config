#!/bin/bash

DOCK=`xrandr | grep "DP3-3 connected"`

if [ -z "$DOCK" ]
then
	if [ -z $(xrandr | grep "DP1 connected") ]
	then
		#DOCK not connected, try HDMI
      		xrandrSetup --hdmi
	else
		xrandrSetup
	fi
else
      xrandrSetup --dock
fi

